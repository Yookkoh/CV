DATABASE_URL="postgresql://cvapp:cvapp_local_pwd@localhost:5432/cvapp?schema=public"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
ADMIN_EMAIL="admin@example.com"
# IMPORTANT: Next.js expands `$VAR` references in `.env*` files.
# Bcrypt hashes contain `$`, so escape them as `\$` or the hash will be mangled.
# Example format: "\$2b\$12\$...."
ADMIN_PASSWORD_HASH="\$2b\$12\$REPLACE_WITH_YOUR_BCRYPT_HASH"
UPLOAD_DIR="public/uploads"
